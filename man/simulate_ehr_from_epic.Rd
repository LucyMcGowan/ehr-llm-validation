% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{simulate_ehr_from_epic}
\alias{simulate_ehr_from_epic}
\title{Simulate EHR Data Extraction from EPIC}
\usage{
simulate_ehr_from_epic(
  epic_raw,
  p_missing = 0.12,
  p_exact = 0.75,
  bmi_noise_sd = 0.6,
  bp_noise_range = -6:6
)
}
\arguments{
\item{epic_raw}{Tibble. Source EPIC data from \code{simulate_epic()}}

\item{p_missing}{Numeric. Proportion of values that fail to extract (default: 0.12)}

\item{p_exact}{Numeric. Proportion of successfully extracted values that are exact matches (default: 0.75)}

\item{bmi_noise_sd}{Numeric. Standard deviation for BMI measurement noise (default: 0.6)}

\item{bp_noise_range}{Integer vector. Range for blood pressure measurement variation (default: -6:6)}
}
\value{
A tibble with the same structure as \code{epic_raw} but with extraction errors:
missing values, measurement noise, and transcription errors
}
\description{
Simulates realistic data extraction errors and noise that occur when extracting
clinical measurements from electronic health records.
}
\details{
Simulates three types of extraction outcomes:
\enumerate{
\item Missing values: Complete extraction failure (proportion = p_missing)
\item Exact matches: Perfect extraction (proportion = p_exact among non-missing)
\item Noisy values: Extraction with measurement error (remaining proportion)
}

Noise models:
\itemize{
\item BMI: Gaussian noise with specified standard deviation
\item Blood pressure: Random integer variation within specified range
}
}
\examples{
epic_data <- simulate_epic()
ehr_data <- simulate_ehr_from_epic(epic_data)

# High error scenario
ehr_data <- simulate_ehr_from_epic(epic_data, p_missing = 0.2, p_exact = 0.6)

}
